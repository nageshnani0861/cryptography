# 31. CMAC Subkey Generation

from Crypto.Cipher import AES

key = b'1234567890123456'
zero = b'\x00' * 16

cipher = AES.new(key, AES.MODE_ECB)
L = cipher.encrypt(zero)

K1 = int.from_bytes(L, 'big') << 1
K1 &= (1 << 128) - 1

print("Subkey K1:", hex(K1))
